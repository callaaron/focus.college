# Cloudflare Pages Redirects

# API routes should NOT be redirected - they are handled by Functions
# Don't add any redirects for /api/*

# SPA fallback - all routes EXCEPT /api/* serve index.html
# Note: This is handled by _routes.json, so we don't need explicit redirect here
