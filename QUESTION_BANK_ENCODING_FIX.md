# 题库管理编码问题修复

## 问题描述

**现象**: 题库管理页面显示全部乱码

**示例乱码**:
```
åœ¨å›¢é˜Ÿä¼šè®®ä¸­ï¼Œæˆ'èƒ½å¤Ÿæ¸…æ™°åœ°è¡¨è¾¾è‡ªå·±çš„è§‚ç‚¹å'Œæƒ³æ³•
```

**应显示**:
```
在团队会议中，我能够清晰地表达自己的观点和想法
```

## 根本原因

题库数据（`assessmentQuestions` 表）在最初插入时使用了错误的字符编码，导致中文字符以非 UTF-8 编码存储，虽然数据库表设置了 `utf8mb4` 字符集，但数据本身已经是乱码。

## 修复方案

### 步骤1: 诊断问题

创建诊断脚本 `scripts/diagnose-question-encoding.ts`：
- 连接数据库并使用 `utf8mb4` 字符集
- 查询前10条题库数据
- 显示题目内容和选项
- 统计题库总数

**诊断结果**:
- 题库总数: 3条（过少）
- 所有题目均为乱码
- 选项内容均为乱码

### 步骤2: 删除乱码数据并重新插入

创建修复脚本 `scripts/fix-question-encoding.ts`：

1. **删除旧数据**:
   ```sql
   DELETE FROM assessmentQuestions
   ```

2. **插入新数据**（使用参数化查询确保UTF-8编码）:
   - 30道题库题目
   - 覆盖10个核心能力（能力ID 1-10）
   - 包含4种题型：
     - `self_assessment` - 自我评估
     - `behavioral` - 行为型
     - `scenario` - 场景型
     - `knowledge` - 知识型
   - 3种难度等级：
     - `easy` - 简单
     - `medium` - 中等
     - `hard` - 困难

### 题库数据结构

每个能力设计了3道题目，题目分布：

| 能力ID | 能力名称 | 题目数量 | 题型分布 |
|-------|---------|---------|----------|
| 1 | 沟通表达 | 3 | 自评(2) + 行为(1) |
| 2 | 团队协作 | 3 | 自评(1) + 场景(1) + 行为(1) |
| 3 | 问题解决 | 3 | 自评(1) + 行为(1) + 场景(1) |
| 4 | 学习能力 | 3 | 自评(1) + 行为(2) |
| 5 | 时间管理 | 3 | 自评(2) + 场景(1) |
| 6 | 需求分析 | 3 | 自评(1) + 行为(1) + 场景(1) |
| 7 | 产品设计 | 3 | 自评(1) + 行为(1) + 场景(1) |
| 8 | 数据分析 | 3 | 自评(1) + 知识(1) + 行为(1) |
| 9 | 用户研究 | 3 | 自评(1) + 行为(1) + 场景(1) |
| 10 | 创新思维 | 3 | 自评(1) + 行为(1) + 场景(1) |

**总计**: 30道题目

### 评分标准

所有题目使用统一的5级评分标准：
- 选项1: 20分
- 选项2: 40分
- 选项3: 60分
- 选项4: 80分
- 选项5: 100分

## 执行修复

```bash
cd /home/user/webapp
npx tsx scripts/fix-question-encoding.ts
```

**修复结果**:
```
✅ 成功插入 30 条题库数据！

前5条数据预览:
ID 4 (能力1): 在团队会议中，我能够清晰地表达自己的观点和想法
  选项1: 从不如此
ID 5 (能力1): 当需要向非技术人员解释技术概念时，我能够使用通俗易懂的语言
  选项1: 完全做不到
ID 6 (能力1): 在冲突情况下，我能够保持冷静并寻求双赢的解决方案
  选项1: 从未如此
ID 7 (能力2): 我能够主动帮助团队成员解决工作中遇到的问题
  选项1: 从不主动
ID 8 (能力2): 在跨部门合作项目中，我能够有效地协调各方资源和需求
  选项1: 完全无法协调
```

## 验证修复

1. **数据库验证**:
   ```sql
   SELECT COUNT(*) FROM assessmentQuestions;  -- 应返回 30
   SELECT * FROM assessmentQuestions LIMIT 5;  -- 中文应正常显示
   ```

2. **前端验证**:
   - 打开题库管理页面
   - 检查题目和选项是否正确显示中文
   - 确认无乱码

## 预防措施

1. **使用参数化查询**: 始终使用参数化查询插入中文数据，避免编码转换问题
   ```typescript
   await connection.execute(insertQuery, [param1, param2, ...]);
   ```

2. **设置连接字符集**: 创建数据库连接时明确指定 `utf8mb4`
   ```typescript
   const connection = await mysql.createConnection({
     uri: DATABASE_URL,
     charset: 'utf8mb4'
   });
   ```

3. **测试验证**: 插入数据后立即查询验证中文显示是否正确

## 相关文件

- `scripts/diagnose-question-encoding.ts` - 诊断脚本
- `scripts/fix-question-encoding.ts` - 修复脚本
- `drizzle/schema.ts` - 数据库表定义（assessmentQuestions）
- `QUESTION_BANK_ENCODING_FIX.md` - 本文档

## 修复时间

**修复日期**: 2025-11-24  
**修复状态**: ✅ 完成  
**影响范围**: assessmentQuestions 表（30条记录）
